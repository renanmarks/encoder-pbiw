FILE(GLOB_RECURSE RVEX_CPP_SOURCES "*.cpp")
FILE(GLOB_RECURSE RVEX_C_SOURCES "*.cc")

LIST(APPEND RVEX_SOURCES ${RVEX_CPP_SOURCES})
LIST(APPEND RVEX_SOURCES ${RVEX_C_SOURCES})

ADD_LIBRARY(RVEX_MODULE STATIC ${RVEX_SOURCES})

target_link_libraries(RVEX_MODULE PBIW_MODULE)	# Como linkar
